# Discord Bot Token — get yours at https://discord.com/developers/applications
DISCORD_TOKEN=

# Optional: restrict slash commands to one guild for instant dev registration
# Remove or leave blank for global commands (takes ~1 hour to propagate)
DISCORD_GUILD_ID=

# Health-check server port (used by Railway / cloud hosts). Default: 8080
PORT=8080
